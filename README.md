# Review Engineering Assistant

Production Next.js version with server-side OpenAI generation.

## Vercel setup

Add `OPENAI_API_KEY` as a sensitive environment variable for Production and Preview. Optionally set `OPENAI_MODEL`; the default is `gpt-5.5`.

Never commit a real API key to this repository.
